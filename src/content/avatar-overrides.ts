/**
 * Q 版形象的「外链覆盖」表。
 *
 * 默认情况下每个模型的形象由脚本按厂商母题确定性合成（见 scripts/avatars/），
 * 好处是全自动、不断档、没有版权负担。
 *
 * 但如果某家厂商发布了官方 Q 版形象、或者你拿到了授权素材，
 * 就在这里加一行，把地址（或 public/ 下的本地路径）指过去即可——
 * 生成器会跳过这个模型的合成，直接用它。**仓库存的是地址，不是图片本体**：
 * 既不用把别人的美术资产搬进来，换图也不需要改代码、重新构建。
 *
 * 键可以是模型 key（`openai|gpt-5.2`）或 slug（`openai-gpt-5-2`）。
 * 这里默认留空——空表意味着全站 100% 由脚本合成，这是可再生的那一档。
 */
export const AVATAR_OVERRIDES: Record<string, string> = {};
